/**
 * Single horizontal inset for Header, hero, and every section so left/right
 * padding stays consistent across breakpoints (~10px mobile → larger on desktop).
 */
export const PAGE_CONTAINER =
  'mx-auto w-full max-w-[1440px] min-w-0 px-2.5 sm:px-5 md:px-6 lg:px-8 xl:px-10 2xl:px-14'
